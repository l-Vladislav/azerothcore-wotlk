__pycache__/
*.pyc

# Файлы карты погоды: подложка — арт Blizzard, контуры зон производны от
# клиентских данных. Собираются локально: scripts/build-map-assets.ps1
app/static/maps/
# У нового фронта своя копия тех же файлов: build-map-assets.ps1 пишет
# только в app/static/maps, оттуда их и копируют.
frontend/public/maps/

# Зависимости стендов (jsdom и прочее): package.json тут есть, а node_modules
# ставится на месте - `npm install` в tools/admin-panel.
node_modules/
